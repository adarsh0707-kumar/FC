{
  "name": "fc",
  "private": true,
  "version": "1.0.0",
  "description": "Football Club full-stack club website (React + Express + PostgreSQL/Prisma)",
  "scripts": {
    "dev": "concurrently -n CLIENTS,SERVER -c blue,green \"npm run dev --prefix clients\" \"npm run dev --prefix server\"",
    "install:all": "npm install --prefix clients && npm install --prefix server",
    "seed": "npm run prisma:seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
